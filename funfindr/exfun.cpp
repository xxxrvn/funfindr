#include "exfun.h"

bool exfun::fastrcmp(std::string a,std::string b){
//    if (a[0]!=b[0])return false;
//    if (a.size()!=b.size())return false;
//    if (a[a.size()-1]!=b[a.size()-1]) return false;
//    if (a[1]!=b[1])return false;
    return (a==b);

  }
